# AIエージェント開発規約: `text2speech` プロジェクト

このファイルは、本プロジェクト（`text2speech`）において作業を行うすべてのAIエージェント（Antigravity CLI, Codex, GitHub Copilot, その他）が遵守すべき共通の開発規約・設計ポリシーをまとめたものです。

開発やコード変更、ドキュメント修正を行う前に、必ずこの内容を一読し、すべて従ってください。

---

## 1. プロジェクト基本方針
- **概要**: Markdown形式の日本語テキストファイルを読み込み、Google AI Studio（Gemini API）を用いて高品質な音声ファイル（WAV）を生成するLinux CLIツール `t2s`。
- **ポータビリティの最大化**: ユーザー環境に `ffmpeg` などの追加バイナリのインストールを要求せず、Python標準ライブラリ（`wave` 等）のみで音声をシームレスに結合・保存する設計を維持します。

---

## 2. 設計・テクニカルポリシー

コードを変更する際は、以下の極めて重要な設計要素を破壊しないよう、慎重に行ってください。

### 2.1. 遅延インポート (Lazy Import) の徹底 🌟
- **ルール**: `google-genai` およびその依存ライブラリ（`pydantic`, `httpx` 等）のインポートは、**実際にAPIを呼び出して音声生成を行う直前**まで絶対に行わないでください。
- **理由**: これらのライブラリはロード時間が非常に長いため、スクリプトの冒頭で無条件にインポートすると、`./t2s -h` や無効な引数を指定しただけの簡単なパース処理の実行に10秒前後の遅延が発生してしまうためです。

### 2.2. 自己再実行 (Self Re-exec) ロジックの維持
- **ルール**: スクリプトの起動直後に行われる、自身と同じディレクトリにある仮想環境 (`.venv`) のPythonインタプリタを自動探索し、`os.execv` で再起動するロジックを必ず維持してください。
- **理由**: これにより、ユーザーが仮想環境をアクティベート (`activate`) していない状態や、システム標準の `python3` で直接コマンドを叩いた場合でも、ライブラリのインポートエラーや名前空間の競合を防ぎ、完全にシームレスなネイティブコマンドとして動作させるためです。

### 2.3. 音声フォーマットと結合仕様
- **出力形式**: 出力は常に **24kHz, 16bit, モノラル PCM** を有した正確なWAVファイルヘッダーを付与して保存します。
- **分割と結合**: 入力テキストを自然な文の切れ目で最大 `chunk_size`（デフォルト 1000文字）に分割し、順次APIにリクエストして得られたPCMバイトデータをノイズなくシームレスに結合してください。

---

## 3. Gitコミットメッセージ規約 📝

本プロジェクトでソースコードやドキュメントを変更し、Gitコミットを作成する場合は、以下のルールを必ず遵守してください。

- **言語**: **コミットメッセージは必ず「日本語」で記述してください。** （英語での記述は禁止します）
- **スタイル**: 簡潔かつ分かりやすく、何を変更したのか、その理由や背景が人間（ユーザー）に一目で伝わるメッセージにしてください。
- **構成例**:
  ```text
  [機能名/ファイル名]: 変更内容の短い要約 (日本語)

  - 変更した具体的な内容 1 (日本語)
  - 変更した具体的な内容 2 (日本語)
  ```

---

## 4. ドキュメントの同期
- 実装コードに変更を加えた場合、またはCLIのオプションを変更した場合は、必ずプロジェクトルートにある `README.md` も同期して更新してください。
